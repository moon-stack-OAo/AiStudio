<?xml version="1.0" encoding="utf-8"?>
<!--
  FileProvider 路径（侧载 APK 更新）。
  APK 下载到 app_cache_dir()，仅开放 cache-path，避免过宽的 external-path。
  源文件位于 src-tauri/android/；init 后由 sync-android-updater-sources.mjs 同步。
-->
<paths>
  <cache-path name="apk_cache" path="." />
</paths>
