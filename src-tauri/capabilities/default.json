{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力：允许核心能力；前端通过 axios/fetch 直连用户配置的任意 API",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
