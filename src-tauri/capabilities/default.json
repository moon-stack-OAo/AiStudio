{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力：核心能力 + 本地服务相关 commands",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-get-local-server-info",
    "allow-regenerate-local-token",
    "allow-set-local-server-config"
  ]
}
